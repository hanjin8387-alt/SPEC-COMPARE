# Test Coverage Agent — 누락 테스트 작성
model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
You are a test coverage agent for PdfSpecDiffReporter (.NET 8 C#).

YOUR TASK — Write ALL missing tests in PdfSpecDiffReporter.Tests/:

T-1: SecureIngestionTests.cs [NEW]
     — Test LoadToMemory() with valid temp PDF file
     — Verify MemoryStream position is 0 after load
     — Verify source FileStream is closed (no file lock remains)
     — Test with empty string path → ArgumentException

T-2: TextExtractorTests.cs [NEW]
     — Test ExtractPages() with null → ArgumentNullException
     — Test with empty MemoryStream (mock as needed)
     — Verify words list and page numbers are populated

T-3: InputValidatorTests.cs [NEW]
     — ValidatePdfPath: null, empty, wrong extension, missing file, valid file
     — ValidateThreshold: NaN, Infinity, 0, 1, 0.5, -1, 2
     — ValidateOptionalConfigPath: null (valid), wrong ext, missing, valid
     — ValidateOutputPath: null, blank, wrong ext, valid

T-4: ChapterMatcherTests.cs [EXTEND]
     — Test with deeply nested children (3+ levels)
     — Test with 50+ chapters to verify O(n²) doesn't time out

T-5: DiffEngineTests.cs [EXTEND]
     — Test with empty Content chapters
     — Test similarityThreshold at exact boundary (0.0, 1.0)

T-6: ProgramIntegrationTests.cs [NEW]
     — Test with --help flag → exit code 0
     — Test with missing arguments → non-zero exit code
     — Test with invalid threshold → exit code 2

T-7: ExcelReporterTests.cs [EXTEND]
     — Verify Unmatched sheet has correct row count
     — Verify Added/Deleted rows have correct color coding

TEST RULES:
- Use SYNTHETIC data only (no real PDFs)
- All temp files must be cleaned in try/finally
- Run: dotnet test --configuration Release --verbosity normal
- 0 failures required

NEVER log simulated document content.
"""
